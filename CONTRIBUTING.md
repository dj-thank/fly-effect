# Fly Effectへの参加

専門分野や経験を問いません。小さな修正、翻訳、再現できなかった報告も重要な貢献です。

## 最初の一歩

READMEの人工回路デモとテストを実行してください。大型データは不要です。取り組みたい変更をIssueで相談し、小さなPRに分けます。日本語・英語の片方だけの改善も歓迎します。

    python -m pip install -e ".[dev,neural]"
    python -m pytest
    python scripts/audit_release.py

## 研究・モデルの変更

変更前に、仮説、出典、対象データ/実ID、変更パラメーター、seed、時間/メモリ予算、成功条件、反証条件を記録します。[実験記録の例](examples/experiment.json)を使えます。

数値一致、身体の実現可能性、神経経路の寄与、行動の成立を分けて検証してください。複数層を同時に変更せず、失敗結果も残します。

原データを上書きせず、派生パラメーターは別ファイルへ保存します。大型データ、メッシュ、論文、重みを権利確認前にPRへ入れないでください。ハッシュと取得手順を共有します。

## 小さく完結する作業サイクル

[現状](docs/STATUS.md)と[依存順序](docs/ROADMAP.md)から一つの阻害要因を選びます。着手時にIssueへ担当・基点SHA・ブランチ・変更ファイル・次の引き継ぎを書き、同じ範囲の作業が既にないか確認します。これは協調ルールであり、自動ロックではありません。

実行前に比較対象と判定条件を固定し、新規workspaceで有界な実験を行います。結果と証拠から次の一手を決めてから、別の仮説へ進みます。否定的な結果も「何を除外でき、何は未確定か」を書けば有用な成果です。同一アプローチで2サイクル新しい識別証拠が出なければ、実験設計を見直します。

次の具体例は[FE-02のA/B計画](examples/fe02-root-compliance-ab.json)です。既存の形式検査をそのまま利用します。

    python scripts/check_experiment.py examples/fe02-root-compliance-ab.json

このコマンドは最小構造の検査だけで、実験を実行せず、追加フィールドの意味・予算の強制・証拠の真実性を保証しません。新しい管理サービスや達成率計算器は導入しません。

記録の`status`は既存の`planned/completed/failed`を維持します。実行完了と科学的な合格は別です。研究上の判定（合格、不合格、判定不能、環境等で阻害、未実施）は別欄に記載します。時間切れや依存不足を能力の不合格に置き換えないでください。

## 証拠と引き継ぎ

再現コマンド、code/model/data/条件の識別子、run/artifact ID、SHA-256、実行環境と未実施項目を残します。保護ファイルは変更前後で照合し、派生身体には新しい識別子と取得・変換履歴を付けます。元のチェックポイントの識別子を書き換えて互換性を偽装しません。

研究PR #14のCIは証拠を14日保管する設定です。期限付きartifactだけを恒久的な根拠にせず、判定に必要な小さなサニタイズ済み結果・条件・ハッシュをレビュー可能な形で保存します。大きな原データは権利確認済みの保存先と再取得・再生成手順を使い、無断でGitへ追加しません。原証拠を失った場合は再検証できない範囲を明記します。

[GitHubのartifact仕様](https://docs.github.com/en/actions/tutorials/store-and-share-data)ではdigest不一致は警告になるため、ダウンロードできたことだけで一致を認定しません。重要な結果ではdigestの明示的な照合と、主張を支える数値の独立再計算を区別して記録します。

## PR

共有版は保護された`main`です。変更は作業ブランチからPRにし、必須チェックとレビューを通して取り込みます。取り込み済みのブランチは整理し、変更履歴はPRに残します。[全体像と公開Issue](docs/PROJECT_OVERVIEW.ja.md)で対象範囲と前提を確認してください。

一つのPRは一つの判断に絞ります。診断、身体の条件変更、神経統合、運用改善を不用意に混ぜず、依存PRと基点を明記します。後から新しい問いが増えたら、その完了条件を別のPRへ切り出します。CI成功・研究上の合格・mainへの統合を別々に報告します。

問題と変更後の動作、実行した確認、残る仮定を簡潔に書いてください。出典とライセンスを明記します。AI支援の使用は構いませんが、検証と権利確認は提出者が責任を持ち、生成した測定値を実測として扱いません。

少人数の運営のため返答期限は保証できません。大規模な変更・依存追加・高コスト実験は事前に相談します。課金サービスの利用は貢献の必須条件にしません。

提出する変更は、そのファイルに適用されるプロジェクトライセンスで提供できるものに限ります。第三者の帰属表記とライセンスを保持してください。
